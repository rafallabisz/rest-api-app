import React from 'react';
import { Alert } from 'reactstrap';

const ErrorPage = () => {
    return (
        <Alert color="danger">
            Error occurated! Please refresh the page or try again later!
      </Alert>

    );
}

export default ErrorPage;